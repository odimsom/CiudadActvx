// Mapbox configuration - No longer needed as mapbox-gl-js doesn't collect invasive telemetry
export const configureMapbox = () => {
  // No configuration needed - mapbox-gl-js only collects minimal technical analytics
};
