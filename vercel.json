{
  "buildCommand": "cd apps/landing && pnpm install && pnpm run build -- --config astro.config.vercel.mjs",
  "outputDirectory": "apps/landing/dist",
  "framework": "astro",
  "installCommand": "pnpm install",
  "devCommand": "cd apps/landing && pnpm run dev",
  "env": {
    "PUBLIC_MAPBOX_TOKEN": "@mapbox_token"
  }
}
