import React from 'react';

// Componente simplificado sin notificaciones automáticas falsas
export const RealtimeNotifications: React.FC = () => {
  // Por ahora no mostramos notificaciones automáticas
  // Solo se activarán cuando haya incidentes reales
  return null;
};
